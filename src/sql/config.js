const config = {
  // 启动端口
  port: 3000,

  // 数据库配置
  database: {
    test: {
      DATABASE: 'test',
      USERNAME: 'root',
      PASSWORD: 'weiying199125',
      PORT: '3306',
      HOST: 'localhost'
    }
  }
}

module.exports = config